services:  
  postgres:
    image: postgres
    container_name: nestjs_postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: spot      

    volumes:
      - spot:/var/lib/postgresql

volumes:
  spot:
